---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const styles = cn(Astro.props.className);
---

<video loop controls class={styles}>
  <source src={"/catch/catch.mp4"} type="video/mp4" />
</video>
