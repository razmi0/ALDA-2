---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<h2>{title}</h2>
<div class="overflow-auto leading-5">
  <p class="max-w-[60ch]">{description}</p>
</div>
